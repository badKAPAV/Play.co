import 'package:flutter/material.dart';

class ResultsAlbums extends StatelessWidget {
  const ResultsAlbums({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
